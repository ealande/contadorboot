{
  "name": "bootstrap5-app",
  "version": "0.0.1",
  "description": "",
  "keywords": [
    "bootstrap",
    "bootstrap5"
  ],
  "license": "MIT",
  "author": "Erick Lima",
  "scripts": {
    "build": "parcel build contador.html",
    "dev": "parcel contador.html",
    "start:front": "npm run build && npm run dev",
    "start:back": "cd server && npm start",
    "start": "./node_modules/.bin/npm-run-all --parallel start:front start:back",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bootstrap": "^5.3.1",
    "bootstrap-icons": "^1.10.5"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "parcel": "^2.9.3"
  }
}
